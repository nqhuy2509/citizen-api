export enum RoleAdmin {
	SUPER_ADMIN = 'super_admin',
	ADMIN = 'admin',
}

export enum UserEnum {
	PENDING = 'pending',
	VERIFIED = 'verified',
	SUSPENDED = 'suspended',
}

export enum Gender {
	MALE = 'male',
	FEMALE = 'female',
	OTHER = 'other',
}

export enum TypeApartment {
	studio = 'studio',
	oneBedroom = 'one_bedroom',
	twoBedroom = 'two_bedroom',
	threeBedroom = 'three_bedroom',
}

export enum StatusApartment {
	AVAILABLE = 'available',
	OCCUPIED = 'occupied',
	MAINTENANCE = 'maintenance',
}
